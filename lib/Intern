const Employee = require('./Employee')

class Intern extends Employee {k
        
        howdy(name, id, email);

        this.school = school;
    }

    getschool() {
        return "School";kk
    }
    
    getRole() {
        return "Intern";
    }
}

    module.exports = Intern;